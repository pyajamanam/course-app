import { EmailCustomValidatorDirective } from './email-custom-validator.directive';

describe('EmailCustomValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailCustomValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
